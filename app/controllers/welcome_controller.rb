class WelcomeController < ApplicationController

  # GET /welcome
  def index
    # hg = HistoryGrabber.new
    # id = hg.query_summoner_by_name("xbanthur")[0].id
    # rv = Array.new
    # @workdone = hg.query_history(id)["matches"].map { |m| m["matchId"]}.collect.to_a
  end

end
